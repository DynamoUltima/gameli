import { Moon, Sun } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useEffect, useState } from "react";

export const ThemeSwitcher = () => {
  // Theme switcher is disabled - dark mode only
  return null;
};
